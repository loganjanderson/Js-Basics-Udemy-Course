// First Functions Challenge

const convertFahrenheitToCelcius = function (temp) {
  let celcius = ((temp - 32) * 5) / 9;
  console.log(celcius);
};

convertFahrenheitToCelcius(32);
convertFahrenheitToCelcius(68);
